# EmmaTresor Backend Dependencies
# ===============================
# These are the Python packages required for the EmmaTresor backend.
# Each package includes version constraints for security and stability.

# Core Django Framework
Django>=5.2.0,<6.1.0                          # Main web framework (latest stable)

# REST API Framework
djangorestframework>=3.16.0,<4.0.0            # Django REST Framework for API endpoints

# Authentication & Security
djangorestframework-simplejwt>=5.3.0,<6.0.0  # JWT token authentication
argon2-cffi>=23.1,<26.0                      # Password hashing (most secure algorithm)
django-axes>=6.5.0,<9.0.0                    # Login attempt protection/brute force prevention

# Web Security Headers
django-cors-headers>=4.3.0,<5.0.0            # Cross-Origin Resource Sharing headers
django-csp>=4.0.0,<5.0.0                     # Content Security Policy headers

# API Enhancement
django-filter>=25.1,<26.0                     # Advanced filtering for REST API

# Image & Media Processing
Pillow>=11.3.0,<13.0.0                      # Image processing library for item photos

# QR Code Generation
qrcode[pil]>=8.2,<9.0.0                      # QR code generation for asset tags

# Input Sanitization
bleach>=6.0.0,<7.0.0                         # HTML sanitization for XSS prevention

# Database & Deployment
psycopg[binary]>=3.2.10,<4.0.0               # PostgreSQL database adapter
gunicorn>=23.0.0,<25.0.0                    # Production WSGI server

# Development & Configuration
python-dotenv>=1.0.0,<2.0.0                  # Environment variable management (.env files)
