/**
 * This barrel file re-exports all public types from the 'auth' module,
 * providing a single, convenient entry point for importing authentication-related types.
 */
export type { LoginRequest, LoginResponse, RegisterRequest, UserProfile } from './auth';
